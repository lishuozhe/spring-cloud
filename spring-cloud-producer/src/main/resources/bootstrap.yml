spring: 
  cloud: 
    config:
      name: config
      profile: dev
      label: master
      discovery:
        enabled: true
        serviceId: spring-cloud-config

eureka: 
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/