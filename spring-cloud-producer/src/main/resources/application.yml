spring:
    profiles:
        active: dev
---     
spring: 
  application:
    name: spring-cloud-producer
  profiles: dev
server:
  port: 9000
  
eureka: 
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
